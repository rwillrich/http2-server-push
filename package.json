{
  "name": "http-server-push",
  "description": "Exploration of HTTP/2 Server Push with ES modules",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "nf start",
    "generate-dependencies": "madge --json ./public > ./src/dependencies.json"
  },
  "engines": {
    "node": "^8.6.0",
    "npm": "^5.3.0"
  },
  "dependencies": {
    "express": "^4.16.1",
    "foreman": "^2.0.0",
    "mime": "^2.0.3",
    "spdy": "^3.4.7"
  },
  "devDependencies": {
    "madge": "^2.2.0"
  }
}
