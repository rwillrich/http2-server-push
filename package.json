{
  "name": "http-server-push",
  "description": "Exploration of HTTP/2 Server Push with ES modules",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node --expose-http2 ./src/http-2",
    "start-http": "node ./src/http"
  },
  "engines": {
    "node": "^8.6.0",
    "npm": "^5.3.0"
  },
  "dependencies": {
    "mime": "^2.0.3"
  }
}
